# CI platform label shown in Slack/email
PLATFORM=web

# Node version for CI
NODE_VERSION=20

# Command to run your tests
# e.g., npm run RegTest-Chrome OR npx playwright test --reporter=line,allure-playwright
TEST_COMMAND=npm run Test-Chrome

# Optional: customize subject prefix for the email
EMAIL_SUBJECT_PREFIX=Acme Web Automation

# Optional: if you use a custom Pages domain/path, set full base URL (no trailing slash)
# If empty, URL becomes: https://<owner>.github.io/<repo>
PAGES_BASE_URL=
